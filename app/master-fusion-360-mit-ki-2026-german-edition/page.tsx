/*
 * ARCHIVED — Book page moved to site root (/). This route redirects for old links.
 * Previous full implementation is preserved in git history and app/page.tsx.
 */
import { redirect } from 'next/navigation';

export default function MasterFusion360MitKi2026RedirectPage() {
  redirect('/');
}
